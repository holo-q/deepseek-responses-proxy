[project]
name = "deepseek-responses-proxy"
version = "0.1.0"
description = "Local Responses API shim for Codex -> DeepSeek Chat Completions"
readme = "README.md"
authors = [{ name = "nuck", email = "teoxysoft@gmail.com" }]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
deepseek-responses-proxy = "deepseek_responses_proxy.app:main"

[build-system]
requires = ["uv_build>=0.10.9,<0.12.0"]
build-backend = "uv_build"
